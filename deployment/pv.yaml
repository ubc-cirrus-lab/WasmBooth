apiVersion: v1
kind: PersistentVolume
metadata:
  name: wasm-cgroup-pv
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/sys/fs/cgroup"